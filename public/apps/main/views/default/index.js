export const show = async (params) => {
  // This will be populated with real data from the backend
  console.log('Dashboard view loaded', params)
}

export const hide = async () => {
  // Cleanup if needed
}
